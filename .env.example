# Copy to .env and set APP_SEEDER_ENABLED=true for the first run to seed the database.
APP_SEEDER_ENABLED=false

# Baked into the frontend build (Vite reads env vars at build time, not runtime).
VITE_API_BASE_URL=http://localhost:8080
